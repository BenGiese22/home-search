# Floors, not pins. The pinned versions could not install on the Vercel
# Sandbox image's Python 3.14: playwright 1.47.0 pulls a greenlet with no
# 3.14 wheel, the build fails, and because pip aborts the whole file
# NOTHING else installs either -- a one-line pin taking down every
# dependency. Verified working in a sandbox at playwright 1.62.
playwright>=1.50
python-dotenv>=1.0.1
beautifulsoup4>=4.12.3
requests>=2.32.3
pytest>=8.3.3
anthropic>=0.116.0
# The one exact pin, on purpose. src/turso_db.py's _AUTH_FAILURE matches
# the driver's error text ("HTTP status 401"), which is not an API; a
# release that rewords it would silently retry a refused token. Pure
# Python, so the wheel problem above cannot apply. Bump it deliberately,
# after checking turso_serverless/session.py still raises that text.
turso_serverless==0.1.0
# The commutes stage reads the clock in America/Denver to decide which
# Wednesday 08:15 to ask about. zoneinfo is stdlib, but its *data* is not:
# on a bare image with no tzdata package ZoneInfo("America/Denver") raises
# and the stage dies on its first line. Cheap insurance for the sandbox.
tzdata>=2024.1
